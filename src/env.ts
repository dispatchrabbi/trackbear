const ENV = {
  URL_PREFIX: import.meta.env.URL_PREFIX,
  ENABLE_METRICS: import.meta.env.ENABLE_METRICS,
  PLAUSIBLE_HOST: import.meta.env.PLAUSIBLE_HOST,
  PLAUSIBLE_DOMAIN: import.meta.env.PLAUSIBLE_DOMAIN,
};

export default ENV;
